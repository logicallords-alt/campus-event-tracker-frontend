{
  "name": "campus-event-tracker",
  "version": "1.0.0",
  "description": "Full-stack application for managing inter-college events",
  "private": true,
  "scripts": {
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "start": "cd backend && npm start",
    "serve-frontend": "cd frontend && npm run preview"
  },
  "keywords": [
    "event",
    "tracker",
    "campus",
    "mern",
    "react",
    "express",
    "mongodb"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "18.x || 20.x",
    "npm": "9.x || 10.x"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
